# 仓库协作规则

## 文档路由

- 面向安装和使用者的内容写入 `README.md`。
- 架构、接口、测试和维护说明写入 `docs/DEVELOPMENT.md`。

## 代码约束

- 新写或修改的业务代码默认补充中文逻辑注释，重点解释原因、边界条件、兼容性和风险；明显自解释的一行代码无需注释。
- 不做与任务无关的全仓格式化，不覆盖并发工作区中的已有修改。
- 媒体删除必须进入系统回收站；路径解析必须保持根目录边界检查。
- 收藏和最近是当前真实根目录上的虚拟视图，任何重构都必须保留真实根目录状态。
- 前端异步请求必须尊重面板关闭、取消和请求时序。

## 最小验证

```bash
node --check web/mediabrowser.js
python -m compileall -q __init__.py censor.py
python -m pytest -q
```

开发环境和人工响应式检查说明见 `docs/DEVELOPMENT.md`。
